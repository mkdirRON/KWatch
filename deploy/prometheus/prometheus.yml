global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: kwatch
    static_configs:
      - targets: ["kwatch:9090"]

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
